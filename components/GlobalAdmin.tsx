"use client";
import { useState } from "react";
import Link from "next/link";
import { Activity, ArrowUpRight, Bell, Building2, CheckCircle2, ChevronRight, CircleDollarSign, Crown, Database, FileClock, Gauge, LayoutDashboard, LogOut, Menu, MoreHorizontal, Plus, Search, Settings, ShieldCheck, UserCog, Users, X } from "lucide-react";
import { toast } from "sonner";
import { createClient } from "@/lib/supabase/client";
import { useRouter } from "next/navigation";

const nav = [["Visión global",LayoutDashboard],["Campañas",Building2],["Usuarios",Users],["Planes y consumo",CircleDollarSign],["Auditoría",FileClock],["Salud del sistema",Activity],["Configuración",Settings]] as const;
const campaigns = [
  {name:"Alcaldía Montería 2027",owner:"Alejandro Doria",plan:"Campaña Pro",users:46,voters:"34.280",status:"Activa"},
  {name:"Asamblea Córdoba",owner:"Elena Santamaría",plan:"Campaña Pro",users:28,voters:"18.450",status:"Activa"},
  {name:"Concejo Sincelejo",owner:"Roberto Silva",plan:"Starter Local",users:12,voters:"7.890",status:"Activa"},
  {name:"Gobernación Demo",owner:"María Cárdenas",plan:"Cobertura Nacional",users:84,voters:"126.700",status:"Configurando"},
];

function Metric({label,value,change,icon:Icon,tone="blue"}:{label:string;value:string;change:string;icon:typeof Users;tone?:"blue"|"emerald"|"amber"|"violet"}) { const colors={blue:"bg-blue-50 text-blue-600",emerald:"bg-emerald-50 text-emerald-600",amber:"bg-amber-50 text-amber-600",violet:"bg-violet-50 text-violet-600"}; return <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between"><span className={`grid h-11 w-11 place-items-center rounded-xl ${colors[tone]}`}><Icon size={21}/></span><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-[10px] font-black text-emerald-700">{change}</span></div><p className="mt-5 text-3xl font-black tracking-tight text-slate-950">{value}</p><p className="mt-1 text-sm text-slate-500">{label}</p></article> }

export default function GlobalAdmin({ownerEmail}:{ownerEmail:string}) {
  const [active,setActive]=useState("Visión global"); const [mobile,setMobile]=useState(false); const [query,setQuery]=useState(""); const router=useRouter();
  async function logout(){const supabase=createClient();if(supabase)await supabase.auth.signOut();await fetch("/api/global-admin/logout",{method:"POST"});router.replace("/global-admin");router.refresh()}
  const sidebar=<><div className="flex h-20 items-center justify-between border-b border-white/10 px-5"><Link href="/" className="flex items-center gap-3 font-black text-white"><span className="grid h-10 w-10 place-items-center rounded-xl bg-gradient-to-br from-amber-300 to-amber-500 text-slate-950"><Crown size={20}/></span><span>Control Global</span></Link><button onClick={()=>setMobile(false)} className="text-slate-400 lg:hidden"><X/></button></div><div className="px-4 py-5"><p className="px-3 text-[10px] font-black uppercase tracking-[.16em] text-slate-600">Administración de plataforma</p><nav className="mt-3 space-y-1">{nav.map(([name,Icon])=><button key={name} onClick={()=>{setActive(name);setMobile(false)}} className={`flex w-full items-center gap-3 rounded-xl px-3 py-3 text-sm font-bold ${active===name?"bg-blue-600 text-white shadow-lg shadow-blue-950/20":"text-slate-400 hover:bg-white/5 hover:text-white"}`}><Icon size={18}/>{name}</button>)}</nav></div><div className="mt-auto border-t border-white/10 p-4"><div className="mb-3 rounded-xl bg-white/5 p-3"><p className="text-xs font-bold text-white">Propietario global</p><p className="mt-1 truncate text-[10px] text-slate-500">{ownerEmail}</p></div><button onClick={logout} className="flex w-full items-center gap-3 rounded-xl px-3 py-2 text-sm text-slate-400 hover:text-white"><LogOut size={17}/>Cerrar sesión global</button></div></>;
  return <div className="min-h-screen bg-[#f4f7fb]"><aside className="fixed inset-y-0 left-0 z-40 hidden w-64 flex-col bg-[#071326] lg:flex">{sidebar}</aside>{mobile&&<aside className="fixed inset-y-0 left-0 z-50 flex w-72 flex-col bg-[#071326] shadow-2xl lg:hidden">{sidebar}</aside>}<div className="lg:pl-64"><header className="sticky top-0 z-30 flex h-20 items-center justify-between border-b border-slate-200 bg-white/90 px-5 backdrop-blur-xl md:px-8"><div className="flex items-center gap-3"><button onClick={()=>setMobile(true)} className="rounded-xl border border-slate-200 p-2 lg:hidden"><Menu/></button><div><p className="text-[10px] font-black uppercase tracking-[.15em] text-blue-600">Administración global</p><h1 className="text-xl font-black text-slate-950">{active}</h1></div></div><div className="flex items-center gap-3"><button className="relative rounded-xl border border-slate-200 p-2.5 text-slate-500"><Bell size={19}/><i className="absolute right-2 top-2 h-2 w-2 rounded-full bg-red-500"/></button><div className="hidden text-right sm:block"><p className="text-sm font-black text-slate-900">Ober Osorio</p><p className="text-xs text-amber-600">Propietario</p></div><span className="grid h-10 w-10 place-items-center rounded-xl bg-amber-100 font-black text-amber-700">OO</span></div></header><main className="p-5 md:p-8">{active==="Visión global"&&<Overview setActive={setActive}/>} {active==="Campañas"&&<Campaigns query={query} setQuery={setQuery}/>} {active==="Usuarios"&&<UsersView/>} {active==="Planes y consumo"&&<Plans/>} {active==="Auditoría"&&<Audit/>} {active==="Salud del sistema"&&<Health/>} {active==="Configuración"&&<Configuration/>}</main></div></div>;
}

function Overview({setActive}:{setActive:(value:string)=>void}) { return <><div className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><h2 className="text-3xl font-black tracking-tight text-slate-950">Toda la plataforma, en una vista.</h2><p className="mt-2 text-slate-500">Rendimiento, clientes y operación de Campaña Ganadora AI.</p></div><button onClick={()=>setActive("Campañas")} className="inline-flex items-center justify-center gap-2 rounded-xl bg-blue-600 px-5 py-3 text-sm font-black text-white shadow-lg shadow-blue-200"><Plus size={18}/>Nueva campaña</button></div><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Campañas activas" value="24" change="+3 este mes" icon={Building2}/><Metric label="Usuarios activos" value="1.284" change="+12,8%" icon={Users} tone="violet"/><Metric label="Votantes gestionados" value="2,48 M" change="+184 mil" icon={Database} tone="emerald"/><Metric label="Ingresos recurrentes" value="$12.840" change="+9,3%" icon={CircleDollarSign} tone="amber"/></div><div className="mt-6 grid gap-6 xl:grid-cols-[1.35fr_.65fr]"><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center justify-between"><div><p className="text-xs font-black uppercase tracking-wider text-slate-400">Crecimiento de la plataforma</p><h3 className="mt-1 text-xl font-black">Usuarios activos mensuales</h3></div><select className="rounded-lg border border-slate-200 px-3 py-2 text-xs"><option>Últimos 8 meses</option></select></div><div className="mt-8 flex h-64 items-end gap-3 border-b border-slate-200">{[38,46,43,58,64,72,81,94].map((height,index)=><div key={index} className="flex h-full flex-1 items-end"><span style={{height:`${height}%`}} className="w-full rounded-t-lg bg-gradient-to-t from-blue-700 to-cyan-400"/></div>)}</div><div className="mt-3 flex justify-between text-[10px] font-bold text-slate-400"><span>ENE</span><span>FEB</span><span>MAR</span><span>ABR</span><span>MAY</span><span>JUN</span><span>JUL</span><span>AGO</span></div></section><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center justify-between"><h3 className="text-lg font-black">Estado del sistema</h3><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-black text-emerald-700">Operativo</span></div><div className="mt-6 space-y-5">{[["API y autenticación","99,99%"],["Base de datos","99,98%"],["Almacenamiento E-14","100%"],["Cola de notificaciones","99,95%"]].map(([name,value])=><div key={name}><div className="flex justify-between text-xs"><span className="font-bold text-slate-600">{name}</span><span className="font-black text-emerald-600">{value}</span></div><div className="mt-2 h-1.5 rounded-full bg-slate-100"><div className="h-full w-[99%] rounded-full bg-emerald-500"/></div></div>)}</div><button onClick={()=>setActive("Salud del sistema")} className="mt-6 flex w-full items-center justify-center gap-2 rounded-xl bg-slate-100 py-3 text-sm font-black text-slate-700">Ver diagnóstico <ChevronRight size={16}/></button></section></div><section className="mt-6 rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex items-center justify-between p-6"><div><h3 className="text-lg font-black">Campañas recientes</h3><p className="text-xs text-slate-500">Actividad más reciente en la plataforma</p></div><button onClick={()=>setActive("Campañas")} className="text-sm font-black text-blue-600">Ver todas</button></div><CampaignTable rows={campaigns.slice(0,3)}/></section></> }

function CampaignTable({rows}:{rows:typeof campaigns}) { return <div className="overflow-x-auto"><table className="w-full min-w-[780px] text-left text-sm"><thead className="border-y border-slate-100 bg-slate-50 text-[10px] uppercase tracking-wider text-slate-500"><tr>{["Campaña","Propietario","Plan","Usuarios","Votantes","Estado",""].map(label=><th key={label} className="px-6 py-4">{label}</th>)}</tr></thead><tbody>{rows.map(item=><tr key={item.name} className="border-b border-slate-100 last:border-0"><td className="px-6 py-4 font-black text-slate-900">{item.name}</td><td className="px-6 py-4 text-slate-600">{item.owner}</td><td className="px-6 py-4"><span className="rounded-lg bg-blue-50 px-2.5 py-1 text-xs font-bold text-blue-700">{item.plan}</span></td><td className="px-6 py-4">{item.users}</td><td className="px-6 py-4 font-bold">{item.voters}</td><td className="px-6 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${item.status==="Activa"?"bg-emerald-50 text-emerald-700":"bg-amber-50 text-amber-700"}`}>{item.status}</span></td><td className="px-6 py-4"><button aria-label={`Opciones de ${item.name}`} className="text-slate-400"><MoreHorizontal/></button></td></tr>)}</tbody></table></div> }
function Campaigns({query,setQuery}:{query:string;setQuery:(value:string)=>void}) { const rows=campaigns.filter(item=>item.name.toLowerCase().includes(query.toLowerCase())||item.owner.toLowerCase().includes(query.toLowerCase())); return <><div className="flex flex-col justify-between gap-4 md:flex-row"><div><h2 className="text-3xl font-black">Campañas</h2><p className="text-slate-500">Administre clientes, propietarios, planes y límites.</p></div><button onClick={()=>toast.success("Formulario de nueva campaña listo para conectar")} className="flex items-center justify-center gap-2 rounded-xl bg-blue-600 px-5 py-3 text-sm font-black text-white"><Plus size={18}/>Crear campaña</button></div><div className="mt-7 rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="p-5"><div className="relative max-w-md"><Search className="absolute left-3 top-3 text-slate-400" size={18}/><input value={query} onChange={event=>setQuery(event.target.value)} placeholder="Buscar campaña o propietario" className="w-full rounded-xl border border-slate-200 py-2.5 pl-10 pr-4 text-sm"/></div></div><CampaignTable rows={rows}/></div></> }
function UsersView() { return <><h2 className="text-3xl font-black">Usuarios globales</h2><p className="text-slate-500">Control de cuentas, roles y accesos a campañas.</p><div className="mt-7 grid gap-4 sm:grid-cols-3"><Metric label="Usuarios totales" value="1.284" change="+42" icon={Users}/><Metric label="Administradores" value="68" change="5,3%" icon={UserCog} tone="violet"/><Metric label="MFA habilitado" value="74%" change="+6%" icon={ShieldCheck} tone="emerald"/></div><div className="mt-6 rounded-2xl border border-slate-200 bg-white p-8 text-center"><UserCog size={52} className="mx-auto text-blue-600"/><h3 className="mt-4 text-xl font-black">Directorio central de usuarios</h3><p className="mx-auto mt-2 max-w-lg text-slate-500">Invite usuarios, suspenda accesos y asigne propietarios o administradores a cada campaña.</p><button onClick={()=>toast.info("Directorio preparado para Supabase Auth Admin API")} className="mt-5 rounded-xl bg-blue-600 px-5 py-3 text-sm font-black text-white">Abrir directorio</button></div></> }
function Plans() { return <><h2 className="text-3xl font-black">Planes y consumo</h2><p className="text-slate-500">Ingresos, suscripciones y límites de uso.</p><div className="mt-7 grid gap-5 lg:grid-cols-3">{[["Starter Local","9 campañas","$1.071 MRR","35%"],["Campaña Pro","12 campañas","$3.828 MRR","47%"],["Cobertura Nacional","3 campañas","$2.157 MRR","18%"]].map(([name,count,mrr,share])=><article key={name} className="rounded-2xl border border-slate-200 bg-white p-6"><p className="text-xs font-black uppercase tracking-wider text-blue-600">{share} de clientes</p><h3 className="mt-3 text-xl font-black">{name}</h3><p className="mt-6 text-3xl font-black">{mrr}</p><p className="mt-1 text-sm text-slate-500">{count}</p></article>)}</div></> }
function Audit() { return <><h2 className="text-3xl font-black">Auditoría global</h2><p className="text-slate-500">Eventos administrativos y cambios críticos.</p><div className="mt-7 rounded-2xl border border-slate-200 bg-white p-6">{[["Plan actualizado","Alcaldía Montería 2027 · owner@campana.co","Hace 8 min"],["Usuario suspendido","Gobernación Demo · soporte global","Hace 24 min"],["Nueva campaña creada","Concejo Sincelejo · Ober Osorio","Hace 1 h"],["Política RLS verificada","Sistema automático","Hace 3 h"]].map(([event,detail,time])=><div key={event} className="flex items-center justify-between border-b border-slate-100 py-4 last:border-0"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-lg bg-slate-100"><FileClock size={17}/></span><div><p className="text-sm font-black">{event}</p><p className="text-xs text-slate-500">{detail}</p></div></div><span className="text-xs text-slate-400">{time}</span></div>)}</div></> }
function Health() { return <><h2 className="text-3xl font-black">Salud del sistema</h2><p className="text-slate-500">Disponibilidad, rendimiento e integridad.</p><div className="mt-7 grid gap-4 md:grid-cols-2 xl:grid-cols-4">{[["API","Operativa","118 ms"],["Supabase","Operativa","84 ms"],["Storage","Operativo","99,99%"],["Vercel","Operativo","Global"]].map(([name,status,detail])=><article key={name} className="rounded-2xl border border-slate-200 bg-white p-5"><CheckCircle2 className="text-emerald-500"/><h3 className="mt-4 font-black">{name}</h3><p className="mt-1 text-sm text-emerald-600">{status}</p><p className="mt-4 text-xs text-slate-400">{detail}</p></article>)}</div></> }
function Configuration() { return <><h2 className="text-3xl font-black">Configuración global</h2><p className="text-slate-500">Parámetros generales y seguridad de plataforma.</p><div className="mt-7 grid gap-5 lg:grid-cols-2">{[[Gauge,"Límites predeterminados","Defina votantes, usuarios, consultas AI y almacenamiento por plan."],[ShieldCheck,"Políticas de seguridad","MFA, sesiones, retención, acceso y tratamiento de datos."],[Bell,"Notificaciones globales","Alertas de consumo, facturación, disponibilidad y seguridad."],[Database,"Integraciones","Supabase, correo transaccional, OCR e inteligencia artificial."]].map(([Icon,title,text])=><button key={String(title)} onClick={()=>toast.info(`${String(title)}: módulo listo para configuración`)} className="flex gap-4 rounded-2xl border border-slate-200 bg-white p-6 text-left hover:border-blue-300"><span className="grid h-11 w-11 shrink-0 place-items-center rounded-xl bg-blue-50 text-blue-600"><Icon size={21}/></span><div><h3 className="font-black">{String(title)}</h3><p className="mt-2 text-sm leading-6 text-slate-500">{String(text)}</p></div><ArrowUpRight className="ml-auto text-slate-300" size={18}/></button>)}</div></> }
